{% extends 'base.html.twig' %}
{% form_theme formView 'bootstrap_4_layout.html.twig' %}
{% block title %}
Edition de 
{{product.name}}
{% endblock %}
{% block body %}
<h1> Editer le produit: {{product.name}}</h1>

{{ form_start(formView, {'attr':{'novalidate': 'novalidate'}})}}

{{ form_errors(formView)}}

 {# {{ form_widget(formView)}} #}
 {# Ce qu'il se passe derriere le rudeau de form_widget() #}
 {# {{form_row(formView.name)}} #}
 {# Ce qu'il se passe derriere le rudeau de {{form_row(formView.name)}}  #}
 {# {{form_label(formView.name)}} 
 
{{form_errors(formView.name)}}
{{form_widget(formView.name)}} 
<p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Quae, maxime!</p>
{{form_help(formView.name)}}  #}
<div class="row">
<div class="col">
{{form_row(formView.name)}} 
{{form_row(formView.price)}}
</div>
<div class="col"> 
{{form_row(formView.mainPicture)}}
   {{form_row(formView.category)}}
   {{form_row(formView.shortDescription)}}
   </div>
</div>


 

 <button  type ="submit" class="btn btn-primary">
 <i class="fas fa-save">
 Créer le produit  
 </i>
 </button>
 {{ form_end(formView)}}

{% endblock %}
